/instigator
*.iso
*.img

# local runtime config - never commit
/instigator.yaml

# local capture/debug artifacts (contain real addresses)
tmp/
/cap.*
*.pcap
/serve*.log

# goreleaser build output
/dist/

